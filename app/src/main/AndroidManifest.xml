<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="creativestudioaq.alarmapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity"
            android:theme = "@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            >

        </activity>

        <activity android:name=".MakeAlarmActivity"/>
        <activity android:name=".AlarmSoundActivity"/>
        <activity android:name=".FeelTalkActivity"/>
        <activity android:name=".RepeatNumberActivity"/>
        <activity android:name=".NameActivity"/>
        <activity android:name=".VersionActivity"/>
        <activity android:name=".WhoMakeActivity"/>
        <activity android:name=".AlarmAlertActivity"/>
        <activity android:name=".AlarmPreferencesActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
            </activity>
        <service android:name=".AlarmService"
            android:process=".serviceProcess"
            />
        <receiver android:name=".AlarmAlertBroadcastReciever"/>

        <receiver android:name=".AlarmServiceBroadcastReciever">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
            </receiver>

    </application>

</manifest>
