<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="creativestudioaq.alarmapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/alice_brandicon2"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme = "@style/SplashTheme">

        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".MakeAlarmActivity" android:screenOrientation="portrait"/>
        <activity android:name=".AlarmSoundActivity" android:screenOrientation="portrait"/>
        <activity android:name=".FeelTalkActivity" android:screenOrientation="portrait"/>
        <activity android:name=".RepeatNumberActivity" android:screenOrientation="portrait"/>
        <activity android:name=".NameActivity" android:screenOrientation="portrait" android:theme="@style/Theme.UserDialog"/>
        <activity android:name=".VersionActivity" android:screenOrientation="portrait"/>
        <activity android:name=".WhoMakeActivity" android:screenOrientation="portrait"/>
        <activity android:name=".AlarmAlertActivity" android:screenOrientation="portrait"/>
        <activity android:name=".SimpleDialog" android:screenOrientation="portrait" android:theme="@style/Theme.UserDialog"/>
        <activity
            android:name=".AlarmPreferencesActivity"
            android:screenOrientation="portrait">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
            </activity>

        <service
            android:process=".serviceProcess"
            android:name=".AlarmService"
            android:icon="@drawable/ic_launcher"
            android:label="AliceAlarm"
            >
        </service>
        <receiver android:name=".AlarmServiceBroadcastReciever" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


        <receiver android:name=".AlarmAlertBroadcastReciever" >
        </receiver>

    </application>

</manifest>
